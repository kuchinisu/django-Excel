el archivo de excel el cual es el que da las solicitudes al servidor de django se encuentra en la carpeta excel

the excel file which is the one that gives the requests to the django server is located in the excel folder